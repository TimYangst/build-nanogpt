[project]
name = "build-nanogpt"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "tiktoken>=0.12.0",
    "torch>=2.9.0",
    "transformers>=4.57.1",
]
